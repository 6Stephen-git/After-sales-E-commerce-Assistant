# Legacy 情景归档

首批 pilot 与 batch2 情景（约 40 叶）已自 `pilot/negotiation` 等目录迁入此处，**不再作为功能验证评测默认跑批路径**。

- 评测树：[`eval_tree.yaml`](../../eval_tree.yaml)、[`eval_tree_batch2.yaml`](../../eval_tree_batch2.yaml) 路径已指向本目录。
- 手写 `fixtures/*.json` 已删除；多步快照改由三期 MS 计划承接。
- 新功能验证情景见 `eval/content/scenarios/functional/`（实施中）。
