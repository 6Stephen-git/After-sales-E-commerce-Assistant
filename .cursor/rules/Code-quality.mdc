---
# 始终注入：本规则对所有对话生效，约束本仓库内代码与配套注释、错误处理、命名。
alwaysApply: true
---

# 代码质量规则

## 注释

- 新增或修改的文件（.md除外），必须含中文分段注释，以函数粒度说明代码块的作用。
- 修改代码时同步更新对应注释，保持注释与代码一致。

## 错误处理

- 所有外部调用（API、数据库、文件读写）必须有错误处理。
- 错误信息包含具体原因，用中文输出，便于定位问题。
- 关键步骤执行前后加日志，日志前缀见 docs/Naming.md。

## 命名规范

- 变量、函数、类名规范见 docs/Naming.md。
- 文件和文件夹按功能拆分，命名见 docs/Naming.md。

## 代码风格

-单一路径：同一功能只保留一条主链路，避免规则和模型并行导致重复与冲突。
-迭代即替换：新版本上线后，旧版本逻辑要同步清理，禁止长期共存。
-简洁优先：设计先求可维护、可验证，避免“多一层保险”式过度设计。